## Summary
- Implemented `.github/workflows/ci.yml` that matches `scripts/ci.ps1`.
- Cleaned up `IDE0001` and `IDE0022` formatting violations in `Grid.cs`, `GridTests.cs`, and `Gravity.cs`.
- Verified CI script runs locally (parity check).

## Testing
- [x] Local `ci.ps1` runs successfully.
- [x] `dotnet test` passes.
- [x] Formatting verification passes.
